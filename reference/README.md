# Reference code

This directory contains third-party / upstream code copied in for reading and comparison.

## Included

- `copilot/` – Notes and reference material about Copilot APIs and tool calling
- `copilot-api-tmp/` – Copilot API package references
- `vscode-copilot-chat/` – Upstream VS Code Copilot Chat source snapshot
- `opencode/` – Reserved for https://github.com/anomalyco/opencode (see `reference/opencode/README.md`)

## Updating

For `opencode/`, use:

- `./scripts/fetch-reference-opencode.ps1`
